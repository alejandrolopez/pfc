<h1><%= t("layout.new") %></h1>

<%= render "commons/error_messages", :target => @layout %>

<% form_tag(layouts_path, :method => :post) do %>
  <div class="formulario">
    <div class="formulario-top">
      <div class="formulario-bottom">
        <%= render :partial => "form" %>

        <div class="botones">
          <div>
            <%= submit_tag t("create"), :title => t("layout.create")  %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>

<%= link_to t("back"), layouts_path, :class => "back" %>