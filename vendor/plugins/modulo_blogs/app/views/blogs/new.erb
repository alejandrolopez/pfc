<h1><%= t("blog.new") %></h1>

<%= render "commons/error_messages", :target => @blog %>

<% form_tag(blogs_path, :method => :post) do %>
  <div class="formulario">
    <div class="formulario-top">
      <div class="formulario-bottom">
        <%= render :partial => "form" %>

        <div class="botones">
          <div>
            <%= submit_tag t("create"), :title => t("blog.create")  %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>

<%= link_to t("back"), blogs_path, :class => "back" %>