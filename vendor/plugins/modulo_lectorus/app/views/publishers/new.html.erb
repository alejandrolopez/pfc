<h1><%= t("publisher.new") %></h1>

<%= render "commons/error_messages", :target => @publisher %>

<% form_tag(publishers_path, :method => :post) do %>
  <div class="formulario">
    <div class="formulario-top">
      <div class="formulario-bottom">
        <%= render :partial => "form" %>

        <div class="botones">
          <div>
            <%= submit_tag t("create"), :title => t("publisher.create")  %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>

<%= link_to t("back"), publishers_path, :class => "back" %>