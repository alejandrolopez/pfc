<h1><%= t("noticia.new") %></h1>

<%= render "commons/error_messages", :target => @noticia %>

<% form_tag(noticias_path, :method => :post) do %>
  <div class="formulario">
    <div class="formulario-top">
      <div class="formulario-bottom">
        <%= render :partial => "form" %>

        <div class="botones">
          <div>
            <%= submit_tag t("create"), :title => t("noticia.create")  %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>

<%= link_to t("back"), noticias_path, :class => "back" %>