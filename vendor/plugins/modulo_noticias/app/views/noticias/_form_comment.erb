<%= form_tag comment_noticia_path, :method => :post, :remote => true, :id => "form_comment_noticia_#{@noticia.id}", :style => "display:none;" do %>
  <div>
    <label for="comment_comment"><%= text_area :comment, :comment, :cols => 40, :rows => 8 %></label>
  </div>

  <div class="botones">
    <div>
      <%= submit_tag t("send"), :title => t("send") %>
    </div>
  </div>
<% end -%>