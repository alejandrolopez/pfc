<h1><%= t("noticia.edit") %></h1>

<%= render "commons/error_messages", :target => @noticia %>

<% form_tag(noticia_path(@noticia), :method => :put) do %>
  <div class="formulario">
    <div class="formulario-top">
      <div class="formulario-bottom">
        <%= render :partial => "form" %>

        <div class="botones">
          <div>
            <%= submit_tag t("update"), :title => t("noticia.update") %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>

<%= link_to t("back"), noticias_path, :class => "back" %>